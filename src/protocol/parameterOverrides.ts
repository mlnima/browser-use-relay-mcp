export const actionParameterOverrides = {
  snapshot: "params: includeScreenshot, includeHidden, allFrames, maxElements. The top frame is the default; request allFrames only when needed. Snapshots contain sparse semantic descriptors, omit default states and bounds, and enforce complete MCP-result limits with truthful truncation metadata. Screenshots are returned as MCP image content instead of JSON text.",
  waitNavigation: "params.event may be committed, domContentLoaded, completed, error, historyStateUpdated, or referenceFragmentUpdated; URL filters also apply to SPA/hash events.",
  querySelectorAll: "target.locator.selector or params.selector; offset 0..100000 and limit 1..5000. Returns a page with truncation, nextOffset, encodedBytes, and a 6MiB byte limit.",
  traverse: "params.relationship parent|ancestor|child|descendant|nextSibling|previousSibling|sibling, locator, index.",
  getText: "Returns rendered text only when its bounded source and encoded scalar fit the 6MiB limit; oversized reads fail explicitly.",
  getHTML: "Returns outerHTML only when its bounded subtree and encoded scalar fit the 6MiB limit; oversized reads fail explicitly.",
  getAttribute: "params.name; the encoded scalar has a 6MiB limit and oversized reads fail explicitly.",
  getProperty: "params.name; result must be JSON-serializable within 6MiB and the bounded item/depth limits; Map, Set, and RegExp values are rejected explicitly instead of being corrupted.",
  getSelection: "Returns bounded selected text by default; params.state=true with a target returns bounded target identity, start/end offsets, and full editable value for exact caret/selection revalidation.",
  selectAll: "Browser input non-activatingly focuses the exact target, verifies it remains focused, then returns the observed full target-relative range only if the shortcut reached it.",
  selectRange: "params.start/end are integers from 0 to 1000. Browser input non-activatingly focuses and revalidates the exact target, then returns the observed target-relative range only if both requested offsets were reached.",
  getComputedStyle: "params.properties string[].",
  setText: "target plus params.text; rejects replacement that would remove descendant id/class attributes.",
  setHTML: "target plus params.html; rejects existing or inserted descendant id/class attributes.",
  setAttribute: "target plus params.name/value; id and class names are rejected.",
  removeAttribute: "target plus params.name; id and class names are rejected.",
  setProperty: "target plus params.name/value; id/class/className/classList and outerHTML/outerText replacement are rejected; inner text/HTML replacements enforce identifier guards.",
  setStyle: "target plus params.styles record or params.name/value; optional priority important.",
  createElement: "params.tagName; optional namespace, text or html, attributes, properties; returns a detached elementId.",
  appendElement: "target destination plus params.source ActionTarget/elementId and position beforebegin|afterbegin|beforeend|afterend.",
  removeElement: "target element without id/class in its composed subtree; returns its in-memory elementId after detaching it.",
  moveElement: "target source plus params.destination ActionTarget and optional position.",
  cloneElement: "target source without id/class in the cloned tree; optional params.destination/position and deep boolean; detached clones return elementId.",
  callMethod: "target plus params.name and JSON args[]; identifier-changing attribute and HTML method routes are rejected.",
  evaluate: "params.expression and world MAIN|ISOLATED; side effects are rejected.",
  evaluateReadOnly: "params.expression; side effects are rejected.",
  evaluateOnElement: "target resolves through a read-only child/shadow path in its permitted frame; params.expression receives the exact target as element, optional world MAIN|ISOLATED, side effects rejected.",
  evaluateInFrame: "target.frameId plus params.expression and world MAIN|ISOLATED; optional cdpFrameId from debugger-aware data.",
  executeInFrame: "params.operation document|text|html|selection|query and optional selector/world; source reads fail explicitly above 6MiB before transport.",
  executeInAllFrames: "Uses the executeInFrame operation contract across at most 256 frames and returns bounded frames plus frame/byte truncation metadata within 8MiB.",
  injectCSS: "params.css required; optional origin USER|AUTHOR and allFrames; target may select frame/document.",
  removeInjectedCSS: "Use the same target, css, origin, and allFrames values used by injectCSS.",
  dispatchEvent: "target plus params.type; constructor fields determine mouse/pointer/key/touch/wheel/input/drag/composition behavior.",
  dispatchCustomEvent: "target plus params.type and serializable detail.",
  setInputFiles: "target must be an enabled input[type=file]; browser/native params.files is a non-empty array of absolute browser-device paths, while DOM params.files is a non-empty [{name,mimeType,base64}] array and dispatches input/change with an untrusted FileList. More than one path/file requires multiple; browser/DOM verify the observed FileList count, browser_upload_files also verifies the exact expanded count and aggregate bytes, while a raw browser directory path verifies a non-empty expanded list. Explicit macOS native chooser fallback accepts one path per action.",
  clearFiles: "target must be an enabled input[type=file]. Browser clears it through CDP DOM.setFileInputFiles([]); DOM clears its value and dispatches untrusted input/change events; both verify the observed FileList is empty.",
  readClipboard: "Optional params.formats filters MIME types; result.items is [{types,data:{[mime]:{base64,size}}}]. Browser clipboard data is limited to 16 items, 32 types, and 16MiB aggregate. Explicit native fallback returns bounded text only.",
  writeClipboard: "Use params.text and optional html, or params.items:[{data:{[mime]:{text?:string,base64?:string}}}]. Browser writes are limited to 16 items, 32 types, and 640KiB decoded aggregate, and the full action must fit Chrome's 1MiB host-to-extension envelope; use base64 item values for large or heavily escaped text. Explicit native fallback writes bounded text only.",
  dropFile: "target drop zone; params.files must contain exactly one {name,mimeType,base64} payload.",
  dropFiles: "target drop zone; params.files must contain one or more {name,mimeType,base64} payloads.",
  waitUpload: "target completion element; params completionAttribute/completionValue/completionText/expectedFileCount/stableMs/pollIntervalMs.",
  dragWithData: "target source; params.destination ActionTarget, data record, effectAllowed, dropEffect.",
  multiTouch: "target is the default center; params.points has at most 32 {x?,y?,id?} entries, moves has at most 1000 point arrays or {points:[...]}, and finite top-level pressure/radiusX/radiusY/tiltX/tiltY/twist/tangentialPressure apply uniformly; requested delay totals at most 60000ms.",
  scrollElement: "DOM uses target-element x/y deltas. Native requires an OS-screen coordinate target and an explicit finite nonzero x or y delta; each axis becomes ceil(abs(delta)/stepSize) native wheel ticks with positive stepSize defaulting to 100 and at most 1000 ticks.",
  clickElement: "target identifies the element; optional params.button, modifiers, durationMs, and clickIntervalMs use trusted browser/native input and requested delays total at most 60000ms.",
  fillField: "target must resolve to an enabled compatible editable field before browser input and params.value is limited to 10000 characters; browser/native input engines accept optional slowly and intervalMs with at most 60000ms requested delay, while DOM assigns directly.",
  chooseOption: "target select control; params.value or params.values selects one or several values in the DOM engine.",
  dragElement: "target source; params.destination ActionTarget or toX/toY, with optional finite durationMs and modifiers; browser requested movement totals at most 60000ms.",
  extractTable: "target a table or one of its descendants; params.maxRows up to 1000 and maxCells up to 10000; returns bounded structured cells plus truncation metadata.",
  extractLinks: "params.offset 0..100000 and limit 1..5000; returns bounded visible links plus truncation and nextOffset metadata within 6MiB.",
  findAndClick: "target locator identifies an actionable element; optional pointer params match clickElement.",
  findAndFill: "target locator and params.value identify an actionable editable field; browser/native input engines accept optional slowly and intervalMs, while DOM assigns directly.",
  scrollUntilFound: "target locator is retried while scrolling; maxScrolls is an integer from 0 to 1000, intervalMs from 1 to 60000, and step is optional.",
  clickUntilGone: "target is clicked until absent; maxClicks is an integer from 0 to 1000 and intervalMs from 1 to 60000.",
  startDownload: "params: url required; optional filename, conflictAction, saveAs, headers, method, body.",
  openDownload: "params.path or completed params.downloadId; browser routing resolves the filename and delegates to the native engine, never chrome.downloads.open.",
  revealDownload: "params.path or completed params.downloadId; browser routing resolves the filename and delegates to the native engine.",
  downloadAndWait: "params.download contains startDownload options; params.wait contains waitDownload filters.",
  eraseDownload: "params.query must be a nonempty Chrome DownloadQuery, or params.all=true explicitly erases every download record.",
  deleteHistory: "params.url deletes one URL; startTime and endTime delete a range; deleting all history requires explicit params.all=true.",
  replaceText: "Browser requires a non-empty target selection, revalidates exact target identity, start/end offsets, and bounded text after non-activating focus, then replaces it with required params.text; an explicit empty string deletes the selection. Insert/delete variants preserve the same exact current caret/range. DOM replaces the target selection directly; explicit native uses the current pre-focused OS selection and rejects a target.",
  uploadFile: "Native begin uses transferId/fileName/totalChunks/totalBytes and optional relativePath/directoryGroupId; chunks use chunkIndex/chunkBase64; end requires expectedSha256; finalizeGroup accepts one directoryGroupId; finalizeGroups atomically accepts unique groups[{directoryGroupId,expectedFiles}] and assigns one shared nonrenewing retention deadline after validating every group; cancel uses transferId; cancelMany uses transferIds[]. Unfinalized groups have a renewable 24-hour activity lease capped at 7 days, then finalized members have a 30-minute lease. Each client is limited to 4096 staged files and 8GiB. browser_upload_files stages standalone sources in one synthetic group, renews prior group leases while transfer progress continues, verifies transfer and rollback metadata, returns every transferId, and bounds pathMetadata to 8MiB with explicit truncation counts and reason.",
  downloadFile: "Native read uses path, transferId, chunkIndex, offset, chunkSize; cancel uses transferId without path. browser_download_file validates exact typed metadata, canonical base64, declared size, and every chunk hash before atomically placing the destination.",
  uploadAndWait: "target file input; params.files browser-device paths and optional params.wait completion fields.",
  submitAndWait: "target a visible submit control for trusted browser input; params.waitFor navigation|stable or omitted to race both; params.wait carries stable options.",
  openAndSwitchToNewTab: "params.tab contains chrome.tabs.create properties such as url.",
  retryAction: "params: action, engine, target, params, retries, retryDelayMs; nested retryAction is rejected.",
  startTabCapture: "params.captureId required; optional target/tabId, mediaConstraints, recorderOptions, timeslice clamped to 250..5000ms. Up to 4 captures, 512MiB/capture, 1GiB aggregate, and 60 minutes are retained safely.",
  stopTabCapture: "params.captureId and filename required; optional saveAs and conflictAction. Stopped Blob URLs are capped at 8 and expire after 30 minutes or download completion.",
  readExtensionStorage: "params.area local|sync|session|managed; optional key/keys/defaults object, afterKey, and limit default 100/max 1000. Returns items plus cursor, count, truncation, and an 8MiB aggregate byte budget.",
  writeExtensionStorage: "params.area local|sync|session; params.items or key/value, removeKeys, or clear. Managed storage is read-only, and the local relay.settings control record is reserved for the extension settings UI.",
  deleteLocalStorage: "params.key removes one value; params.all=true explicitly clears the selected frame origin's localStorage.",
  deleteSessionStorage: "params.key removes one value; params.all=true explicitly clears the selected frame origin's sessionStorage.",
  readLocalStorage: "params.key returns one value within 6MiB; omit key for offset/limit pagination with default 1000/max 10000 entries and truncation metadata.",
  readSessionStorage: "params.key returns one value within 6MiB; omit key for offset/limit pagination with default 1000/max 10000 entries and truncation metadata.",
  deleteIndexedDB: "params.database/store and key remove one record; params.all=true explicitly clears the whole object store.",
  inspectCacheStorage: "Optional params.cache selects one existing cache; maxCaches defaults 20/max 100; limit is a total entry budget default 100/max 1000; includeBodies reads at most 1MiB each and 8MiB total with truncation metadata. Opening a concurrently removed cache can recreate it.",
  readCacheStorage: "params.cache/url required; optional maxBodyBytes up to 8MiB. Returns bounded headers and base64 body or fails explicitly when the body exceeds the limit.",
  deleteCacheStorage: "params.cache plus url deletes one entry; params.cache plus all=true explicitly deletes the whole cache.",
  clearSiteData: "params.dataToRemove/dataTypes must enable at least one type; provide explicit options/since/origins/originTypes/excludeOrigins, or params.all=true for all-time scope.",
  clearBrowsingData: "params.dataToRemove/dataTypes must enable at least one type; provide explicit options/since/origins/originTypes/excludeOrigins, or params.all=true for all-time scope.",
  inspectIndexedDB: "Optional params.database returns bounded stores, indexes, and key paths; params.objectStore plus limit 1..10000 and optional afterKey returns rows within 6MiB with count, cursor, truncation, and oversized-record metadata. Map, Set, and RegExp records fail explicitly. Opening may recreate a database deleted concurrently.",
  readIndexedDB: "params.database/store and key return one value within maxBytes up to 6MiB; omit key for a page with limit 1..10000, optional afterKey/maxBytes, encodedBytes, truncation, and nextKey. Map, Set, and RegExp values fail explicitly.",
  setNetworkRules: "scope dynamic|session defaults dynamic; params.rules replaces every rule in that scope, params.addRules adds without replacing, and removeRuleIds removes selected rules.",
  clearNetworkRules: "params.scope dynamic|session|all defaults dynamic and removes every rule in the selected scope.",
  enableResponseBodyCapture: "target.tabId selects the tab; attaches CDP Network and applies only to future requests until the debugger detaches or browser control is disabled.",
  listRequests: "Read-only filters: source webRequest|cdp, target.tabId or params.tabId, method, type, phase, statusCode, urlContains. limit defaults to 100 and is clamped to 1..200; results include matched/returned/truncated and per-source dropped record/byte metadata.",
  getResponseBody: "params.source must be cdp and params.requestId must identify a completed CDP record with bodyAvailable true; maxBodyBytes defaults to and cannot exceed 16MiB, with explicit oversize failure.",
  fetch: "params.url required; optional method, headers, body, credentials, cache, redirect, referrer, referrerPolicy, integrity, keepalive, mode, responseType text|json|base64, and maxResponseBytes up to the 16 MiB hard limit.",
  observePage: "params.sinceRevision waits for a greater revision; omission returns current state.",
  nativeFileChooser: "Assumes chooser is open; params.path or paths[], dialogDelayMs, delayMs. macOS accepts one path per native chooser action.",
  nativeSaveDialog: "Assumes dialog is open; params.path required.",
  nativePrintDialog: "params.open defaults true and sends the platform print shortcut; optional dialogDelayMs and confirm, where false dismisses the dialog.",
  nativePermissionPrompt: "params.decision allow|deny|dismiss and optional tabCount.",
  nativeBrowserUI: "OS target coordinates and/or params.shortcut/text.",
} as const;
